'use client';

import StrategyBoard from '../../../../components/shared/Strategy/StrategyBoard';

export default function ClientStrategyPage() {
    return (
        <div className="h-screen bg-[#050511]">
            <StrategyBoard role="client" />
        </div>
    );
}
